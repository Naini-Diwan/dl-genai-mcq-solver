# Core ML / deep learning
torch==2.10.0
torchao==0.17.0

# Hugging Face stack
transformers>=4.40.0
peft>=0.11.0

# Data handling and classical ML
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.4.0

# Experiment tracking
wandb>=0.17.0

# Note: kaggle_secrets is only available inside the Kaggle notebook
# environment and is not installable via pip. Outside Kaggle, replace
# UserSecretsClient with wandb.login() or the WANDB_API_KEY env var.
